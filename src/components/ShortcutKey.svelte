<!-- <script lang="ts">
	import { onMount } from 'svelte';
	// import * as styles from './ShortcutKey.css';

	let isMacOS = false;

	onMount(() => {
		isMacOS = navigator.userAgent.includes('Macintosh');
	});
</script>

<kbd class={styles.keyboardKey}>
	<slot>
		{#if isMacOS}
			⌘
		{:else}
			⌃
		{/if}
	</slot>
</kbd> -->
